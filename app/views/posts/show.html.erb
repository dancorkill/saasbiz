<h1><a href="<%= @post.url %>"><%= @post.title %></a></h1>
<p>Comments: <%= @post.comments.count %></p>

<% @post.comments.each do |x| %>
	<div class="comment">
		<p><%= x.content %></p>
	</div>
<% end %>

<hr>

<%= render 'commentform' %>