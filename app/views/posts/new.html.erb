<%= form_for (@post) do |f| %>

<div class="inputs">
	<%= f.label "Article Title" %>
	<%= f.text_field :title %></br>
	<%= f.label "URL" %>
	<%= f.text_field :url %>
</div>

<div class="submit">
	<%= f.submit "Submit Boom" %>
</div>
<% end %>